# Changelog

[Keep a Changelog](https://keepachangelog.com/) 스타일. 시점성 진행은 [DEVLOG.md](./DEVLOG.md),
계획은 [ROADMAP.md](./ROADMAP.md) 참조.

## [Unreleased]

### Added
- Implicit-World-Modeling: LlamaFactory 부트스트랩 정본 — `scripts/setup_llamafactory.sh`(`--install --verify`, 멱등) 한 커맨드로 clone→pin→patch→verify. LF 가 **pin 없는·gitignore된·직접 변조되는 서드파티 체크아웃**이라 디렉토리를 지우면 패치된 소스 6파일·학습 YAML 74개·in-place 변조된 `dataset_info.json`·런타임 심링크가 **영구 소실**되던 구조를 git 에서 재구성 가능하게 만들었다: **pin `99464b3d` + `patches/llamafactory/{0001-diff-loss,0002-double-ce-fix}.patch`**(+ `MANIFEST.sha256`)를 pristine clone 에 적용한 결과가 **살아있는 LF 와 byte-exact 일치**(src 6파일 `diff` 무출력). 함께 GPU 정책 SSoT `scripts/gpu_policy.py`(`.env`/노트북 Cell 5·10/`_common.sh` 4곳 분산 → 단일 정본), YAML 생성기 `implicit_world_modeling/{gen_configs,lf_registry}.py`, 원격 GPU 클러스터 제출 스펙 `configs/remote/run.template.yaml` + `scripts/remote_launch.sh`(**제공자 중립** — 코드에 벤더명 0건, 제출 커맨드는 `.env` 의 `REMOTE_SUBMIT_CMD` 템플릿으로 주입; **UNVALIDATED** — 이 머신에 제출 CLI 가 없어 실제 제출은 미검증), 신규 테스트 `tests/{test_gpu_policy,test_gen_configs,test_diff_loss_double_ce}.py` 추가  (2026-07-13)
- Implicit-World-Modeling: 모델 레지스트리에 `qwen3-vl-4b`(`Qwen/Qwen3-VL-4B-Instruct`, template `qwen3_vl_nothink`, size tier `3-4B`) 복원 — 커밋 `67a52e5`(2026-05-31, "qwen3-vl-4b 삭제·7-9B 단일 tier 통합")가 제거했던 모델을 `scripts/_common.sh` 3구조(`MODEL_ID`/`MODEL_TEMPLATE`/`ALL_MODELS`) + 노트북 Cell 5 3구조(`MODEL_FAMILY_CONFIG`/`_MODEL_CONFIG`/`MODEL_ORDER`)에 재등록해 **4 모델 2 tier** 체제가 됐다(7-9B: `qwen3-vl-8b`·`qwen2.5-vl-7b` / 3-4B: `qwen3-vl-4b`·`qwen2.5-vl-3b`). EXP01–EXP04 의 모델 자격을 Qwen3-VL **계열**(4b/8b)로 확장하고, EXP05(절대 픽셀 좌표 840×1876)는 좌표·factor 이중 mismatch 로 Qwen3-VL 계열 전체를 배제 — 이 배제는 코드 가드가 아니라 **문서 규약**이다. **`qwen3-vl-4b` 는 학습 이력이 전혀 없다** (기존 EXP 결과는 모두 다른 모델 기준이고 `!bash` 실행 셀은 byte 불변). hparam delta 없음 — `_SIZE_CONFIG_AC` tier dict 를 빈 dict 로 유지해 EXP01/EXP02 실측 어댑터와 동일조건 보존. 함께 노트북 12개 셀의 stale 서술(8모델/12개모델 카운트 · 96 YAML · tier 3단 · 구식 `custom/Implicit-World-Modeling-{DS}` 경로 · Cell 5 자기모순 주석)을 코드 유도값으로 정합화  (2026-07-13)
- Implicit-World-Modeling: EXP05 데이터 빌드 정본 경로 `scripts/build_exp05_data.py` 신설 — v2 diff-loss 체인(mirror → 토큰 가중 → 원자 교체)에 **커밋된 호출자가 없어** EXP05 train 이 out-of-band 산출물이었고 fresh clone 재현이 불가능했다(GAP1, CRITICAL). 기록이 없던 0710 생성 명령을 **재현 게이트 400/400 완전 일치**로 복원해 스크립트로 고정(model `Qwen/Qwen2.5-VL-3B-Instruct`, template auto→qwen, w 1.0/1.0/0.25, metric v2)하고, tokenizer revision·가중 상수·집계를 `<train>.meta.json` sidecar 에 기록. 신규 `tests/test_diff_loss_v2.py` 10케이스(경계 걸침·zero-length offset·중첩 max·input==output 거부·부분 산출물 미잔류·fallback 미집계 등)  (2026-07-13)
- Monkey-Collector: luminance prefilter(MobileGPT-V2 Stage-0 포팅) — 시각적으로 동일한 화면 재방문 시 LLM element-extraction 0회 단락. `ScreenMatcher.match()` 에 Stage-0c 추가(screenshot luminance 지문 비교, 차이 픽셀 비율 < `screenshot_diff_threshold` 면 기존 `page_key` 로 merge, `match_type=LUMINANCE_PREFILTER`, pending guard 뒤 배치로 blackhole 보호 유지), 신규 `pipeline/screen_matching/luminance.py`(순수 Pillow, numpy 미사용) + `PageKnowledge.luminance_features`(세션 인메모리, page 당 cap 10). 하이퍼파라미터 4종 config 6-place 노출(`luminance_prefilter` 기본 ON·`luminance_threshold`·`screenshot_diff_threshold`·`luminance_low_res_width`: `config.py`·`config/run.yaml`·`MC_SCREEN_MATCHING_*` env·`cli.py`)  (2026-07-01)
- Monkey-Collector: external 복구의 타깃 앱 재실행을 `open_app` 액션으로 events.jsonl 에 기록(`DataWriter.log_open_app`, excursion 당 1회) — open_app 학습용. `return_to_app`/`recover` 가 launch 여부를 `bool` 반환, `cli._resolve_app_names`→`Collector(app_names=...)` 로 `app_name` 조인. navigation 격리 3중(`state.last_action` 클리어·`explorer._last_record` 클리어·`transition:false` + `page_graph._load_events` 스킵) + `DataWriter` 멀티스레드 `threading.Lock`  (2026-06-30)
- `docs/` 루트 문서 허브 + `/project-sync` 설정(`.project-sync.json`) 도입  (2026-06-28)
- Monkey-Collector: OpenRouter 공용 LLM 클라이언트(`llm/client.py`, 기본 `qwen/qwen3.7-plus`) + 화면 의미 그룹핑(`llm/screen_grouper.py`, `--screen-grouping` 플래그)  (2026-06-28)
- Monkey-Collector: `setup-collector` 스킬 `references/` deep-dive 8종(client-build, mediaprojection-accessibility, google-login, run-and-verify, seed-helpers, seed-pim, seed-notes-tasks, seed-media-misc) + 런타임 권한 다이얼로그 adb 자동허용(`collection_loop._try_grant_permission_via_adb`, "While using the app" 우선 탭·deny-guard)  (2026-06-29)
- Monkey-Collector: element-set screen matching 패키지(`pipeline/screen_matching/` — ScreenMatcher·ui_attributes·set_classifier·page_knowledge) + 화면당 단일 LLM 호출 `llm/element_extractor.py`/`llm/prompts/element_extractor_prompt.py`(element_index family + key_element_index anchor 동시 추출, MobileGPT-V2 Node-Clustering 포팅); CLI `--cluster-merge-tolerance`·`--max-expand-iters` 플래그  (2026-06-29)

### Changed
- Implicit-World-Modeling: 학습 설정 소유권을 LlamaFactory 워킹트리에서 **repo 로 이전** — 학습 YAML 정본이 `LlamaFactory/examples/custom/`(gitignore → 유일본)에서 **`configs/train/` 162개**(git 커밋, 생성기 `python -m implicit_world_modeling.gen_configs`)로, dataset_dir 정본이 in-place 변조되던 LF 의 `dataset_info.json` 에서 **`configs/lf_dataset/`**(`dataset_info.json` + 상대 심링크 7)로 옮겨졌다. 런타임에 LF 내부 상태를 **읽지도 쓰지도 않는다**. **커밋 YAML 은 GPU-불변 baseline** 이고 GPU 트리오(pdbs/grad_accum/deepspeed)는 `llamafactory-cli train cfg.yaml key=value` **런타임 override** 로 주입(LF `hparams/parser.py` OmegaConf merge) → **하드웨어를 바꿔도 YAML 재생성 불필요**; RTX5090 {1,2} · A100·H100 {1,2,4,8} 전 조합에서 `GLOBAL_BATCH=64` 유지를 실측했다. deepspeed 는 GPU 무관 **항상 offload** 로 고정 — as-trained **74/74 가 offload** 였고 `ds_z3_config.json`(no-offload)은 **한 번도 실행된 적 없는 죽은 기본값**이라, "A100 이면 offload 를 빼도 된다"는 조건부 분기는 미실행 경로로의 조용한 divergence 였다(A100 에서 빼면 EXP05 7B full FT 는 모델상태만 GPU당 ~77 GiB 로 **확정 OOM**). EXP03/04 YAML 은 **애초에 디스크에 없었고**(전역 `find` 0건 — 노트북 `_YAML_GEN_DS` allowlist 와 "복구 불가, 재생성 금지" 주석이 존재하지 않는 파일을 지키고 있었다) 생성기로 재구성해 `# [reconstructed 2026-07-13]` 헤더를 달았다(원본 소실이라 as-trained 와 다를 수 있음). 노트북은 정본 로직을 전부 코드로 이관하고 **thin wrapper** 가 됐다(Cell 3/5/7/10/12/14)  (2026-07-13)
- Implicit-World-Modeling: EXP05 stage1 데이터 원천을 0710 → **0711 수정본**으로 교체 — 변경의 실체는 **`wait` 액션 클래스 교체 하나**다(기존 `wait` 4,958건 전량 퍼지 + 전혀 다른 (episode,step) 위치에 400건을 action 쪽에만 재생성; 나머지 7개 액션은 건수까지 완전 동일, 공통 73,950건의 변화는 프롬프트·JSON 포맷 정리뿐이라 GT 의미 불변). 재생성 실측: source pool action 74,350 / state 73,950 → mirror train **47,556 → 44,670행**, 총계 **64,787 → 60,717행**, 가중 train = state 31,221 + action 13,449(state 출력 토큰 53.4% 가 0.25배 감쇠, fallback 0건). 0710 원천·구 EXP05 산출물은 `data/AndroidControl/_backup_0710/` 에 보존. **조병웅님 확인 대기 4건**: `wait` 전량 퍼지(퍼지분에 EXP01 train 2,548 · test 598 멤버십 포함 → 오염 제거가 아니라 커버리지 축소, train −6.1%) / 신규 `wait` 400건 중 399건이 빈 current state(74건 train 유입, degenerate shortcut) / action⊋state 키 대칭 붕괴(400 차이) / 좌표 범위이탈 11키  (2026-07-13)
- Monkey-Collector: page matching을 MobileGPT-V2식 LLM element-set matching에서 Mobile3M "Unique Page"(BM25 + conjunctive element/pixel diff)로 교체 — **LLM-free matching**(화면당 LLM 호출 0). 신규 `pipeline/screen_matching/bm25.py`(`Bm25Index`, Okapi k1=1.5/b=0.75, numpy 미사용)·`element_lines.py`(encoded XML→element-line 문서 + `element_diff_count`/`element_jaccard`); `ScreenMatcher.match()` 재작성(구조지문 prefilter → pending 가드 → element-line 직렬화 → BM25 top-K 후보 → conjunctive verify: element diff `<element_diff_max` **AND** `luminance_diff` `<page_pixel_diff_threshold` → `BM25_MERGE`, 아니면 `NEW`; `match_type` NEW/STRUCTURAL_IDENTICAL/BM25_MERGE/PENDING_EMPTY). `ScreenMatch`/`page_key` 출력 계약 불변으로 page_graph·exploration·storage 무변경. config knob 5종 6-place 노출(`bm25_top_k=5`·`element_criterion`·`element_diff_max=5`·`element_jaccard_min=0.5`·`page_pixel_diff_threshold=0.3` + `MC_SCREEN_MATCHING_*` env + CLI). `PageKnowledge.element_lines` additive 직렬화, `rehydrate`가 세션 재개 시 BM25 코퍼스 재구축(legacy page.json은 첫 observation raw.xml로 재계산). 참고 논문(top-5 + diff<5) vs 참고 코드(top-1 argmax + Jaccard>0.5) 불일치는 논문 스펙 기본 + knob 전환. 구 `set_classifier.py`·`cluster_merge_tolerance`/`max_expand_iters` deprecated 존치  (2026-07-02)
- Monkey-Collector: 필터된(prefilter/dedup) 재방문 화면도 저장 — 신규 `screen_matching.persist_filtered`(기본 `on`). 켜지면 `ScreenMatcher`의 3개 재사용 종료지점(structural prefilter hit·luminance prefilter hit·`_record_observation` merge dedup)이 기존 observation 재사용 대신 그 page 아래 새 `observation_num`(방문마다 `0,1,2,…` per-visit 체인)을 할당하고 `is_new_observation=true` 로 반환 → 기존 루프 게이트가 발화해 `save_observation`이 새 디렉토리를 씀(page 정체성·LLM 0회 불변, no-overwrite, prefilter 히트는 luminance 지문 재-append 생략). 매처 단독 변경(collection_loop/storage/rehydrate 무변경) + config 6-place 노출(`--persist-filtered {on,off}`·`MC_SCREEN_MATCHING_PERSIST_FILTERED`). `off` 면 기존 no-write-on-revisit 동작 복원. 앞선 저장 재설계(2026-07-01)의 "재방문 관측 파일 미기록"을 기본값에서 뒤집음  (2026-07-02)
- Monkey-Collector: LLM을 입력 텍스트 생성 전용으로 기본 전환 — `llm.element_extraction` 기본 `true`→`false`, luminance/structural prefilter를 element_extraction에서 분리(extractor 없이도 `luminance_prefilter` on 이면 ScreenMatcher가 prefilter-only 모드로 structural+luminance+observation dedup 유지, element-set 경로 우회). element_extraction off 시 same-function 압축 없이 pure unexplored-first로 탐색 degrade  (2026-07-01)
- Monkey-Collector: 저장 레이아웃을 flat `data/raw/{package}/`에서 `data/`(memory: `pages/{page_key}/{observation_num}`) + `runtime/`(세션 진행: `metadata.json`·`events.jsonl`·`cost.csv`·`activity_coverage.csv`) 이원화로 재설계(MobileGPT-V2 memory/runtime 분리 포팅) — 재방문 관측은 파일 미기록, `screen_matcher._record_observation`이 page-scoped 재사용 판단, 신규 `pipeline/screen_matching/rehydrate.py`가 세션 재개 시 ScreenMatcher/page_graph 지식을 디스크에서 복원(기존 매 세션 강제 reset 버그 수정); `cli.py`의 `--output`/`--raw-dir` → `--data-dir`/`--runtime-dir`, `config.py`의 `output_dir` → `data_dir`/`runtime_dir`  (2026-07-01)
- Monkey-Collector: 입력 텍스트 생성을 OpenAI Responses API(gpt-5-nano)에서 공용 OpenRouter `LLMClient`(Chat Completions)로 이전  (2026-06-28)
- Monkey-Collector: 탐색 엔진을 `SmartExplorer`(화면 단위 weighted-random)에서 `LLMGuidedExplorer`(coverage-driven unexplored-first + LLM same-function 압축 + transition-graph 최단경로 navigation, 신규 `pipeline/exploration/` 패키지)로 전면 교체; App/Server TCP·저장 포맷 유지, `networkx` 의존성 추가  (2026-06-29)
- Monkey-Collector: `setup-collector` 스킬을 SKILL.md 오케스트레이션 + `references/` 구조로 재구성(AVD Pixel6-2, 빌드 JDK17/AGP8.2, MediaProjection 재동의·Google 로그인·더미데이터 시드·라이브 검증 단계 추가, 전 단계 멱등)  (2026-06-29)
- Monkey-Collector: input-text LLM 생성 프롬프트에 현재 앱 설명 주입 — `AppJob.description`(`app_name (category/sub_category) — notes`, csv 미등록 앱은 package_id 폴백)을 CLI `_resolve_app_contexts`→`Collector(app_contexts=...)`→세션마다 `TextGenerator.set_app_context()`로 전달해 앱 도메인에 맞는 입력값 생성; 공유 generator 누수 방지 위해 매 세션 무조건 set, random 전략은 no-op  (2026-06-29)
- Monkey-Collector: 화면 의미 그룹핑(annotation 전용)을 element-set screen matching으로 교체 — 산출 `page_key`가 page_graph 노드(`PageGraph.get_or_create_page_by_match`)와 탐색 abstract page(`SemanticState.page_key`, Memory/TransitionGraph/Navigator 키)를 동시 결정(과거 grouping↔matching 디커플을 커플링); CLI `--screen-grouping` → `--element-extraction {on,off}`(deprecated alias 유지), 산출물 `{step}_groups.json`→`{step}_elements.json`, cost.csv 라벨 `screen_grouper`→`element_extractor`; 키 없거나 off면 `page_key=structure_str` fallback(기존 파이프라인 byte-for-byte)  (2026-06-29)

### Removed
- Implicit-World-Modeling: `scripts/diff_loss/apply_llamafactory_patch.py` — anchor 문자열 치환 방식의 LF 패처 은퇴. 패치 정본이 리뷰 가능한 `patches/llamafactory/*.patch` 커밋 + `scripts/setup_llamafactory.sh` 로 대체됐다(clone 에 커밋 pin 이 없어 upstream HEAD 가 움직이면 anchor 치환이 깨질 수 있던 취약점도 pin `99464b3d` 로 함께 제거)  (2026-07-13)
- Monkey-Collector: `llm/screen_grouper.py` + `tests/unit/test_screen_grouper.py` — element-set screen matching(`pipeline/screen_matching/`)으로 대체  (2026-06-29)

### Fixed
- Implicit-World-Modeling: diff-loss **이중 CE** 수정 — `use_diff_token_weighted_loss` 경로가 `labels` 를 pop 하지 않아 HF 내부 CE 가 **이중 실행되고 그 결과가 버려지던** 버그 + logits 전량 fp32 upcast 를 교정(chunked CE 도입). **activation peak 20.87 → 10.72 GiB (48.6% 절감)**, 결과는 **bit-exact** — chunk 128/333/4096 × `num_items` 유무 전 조합에서 `|Δloss| = 0.000e+00`, `max|Δgrad| = 0.000e+00`(tolerance 없이 `torch.equal`). 함께 재구성 검증 중 잡은 결함 4건: `setup --verify` 가 **스택 패치를 오판**(0002 가 0001 이 추가한 라인을 고치므로 0001 단독 역적용은 원리적으로 불가능 → 항상 FAIL; 기지 상태 byte-exact 대조로 교체), **`dataset_dir` 미전달**(커밋 YAML 의 `dataset: IWM-AC_*` 키가 `configs/lf_dataset` 에만 있어 안 넘기면 **fresh clone 에서 학습이 시작조차 못 함**), **`.env` 가 프로세스 환경을 덮어씀**(`GPU_TYPE=A100 NPROC_PER_NODE=4 bash stage1_train.sh` 가 무시돼 GPU 매트릭스를 쓸 수 없었다), **`save_strategy=no` 가 boolean 파싱**(OmegaConf 는 YAML 1.1 규칙이라 따옴표 없는 `no` 를 `False` 로 읽음). EXP03 stage2 dataset 미등록(데이터 128MB·YAML 12개가 다 있는데 정본에 엔트리가 없어 LF 안쪽에서 죽던 상태)도 등록 + `require_dataset_registered` 가드로 학습 전에 잡히게 했다. 검증 **547 passed / 9 skipped / 실패 0**, GPU 매트릭스 10/10, **1-step 실학습 스모크**(EXP02 3B LoRA) loss 0.3309 · 22초 · `token_weights` 배치 실림 확인  (2026-07-13)
- Implicit-World-Modeling: diff-loss **v2** 체인 버그 4건 수정(v1 4파일은 AC_EXP02 재현성 보존 위해 불가침) — (S2-05) `token_weight_builder_v2` 가 **토큰 시작점만** 검사해 element 왼쪽 경계를 걸친 토큰을 놓치고 오른쪽 넘침엔 주던 **비대칭 경계 판정**을 **interval overlap**(zero-length offset 제외) + 중첩 span **max 가중치**(순서 비의존)로 교정 — 실측 state 토큰의 0.65%가 바뀌었고 **전부 0.25→1.0 상향**(하향 0건)으로 버그 서명과 일치. (S2-03) fail-open → `--on-error {fail,uniform,skip}` 도입, 기본 **fail-closed**, 폴백을 성공으로 집계하지 않음. (S2-09) 최종 경로 직접 스트리밍 → **sibling temp + `os.replace` 원자 교체** + `--input == --output` 거부. (S2-08) `--revision` 으로 tokenizer commit 고정 + sidecar 기록. 검증 **96 passed / 9 skipped**, 신규·수정 파일 ruff clean  (2026-07-13)
- Monkey-Collector: MediaProjection 토큰 단발성 reuse-guard + `createVirtualDisplay` graceful-degrade(`ScreenStabilizer`); `EXCLUDED_PACKAGES`(`CollectorService`)·`SYSTEM_PACKAGES`(`screen_guard`)에 gms/gsf/vending 추가로 외부앱 스톰 차단; no-ACK 세션 abort(`session_manager`)  (2026-06-29)
- Monkey-Collector: 빈 page_0 blackhole 수정 — interactable 0개 화면(첫 로딩/스플래시)을 `ScreenMatcher.match()` entry guard로 `pending` 거부해 page 미등록(첫 유효 화면이 page_0), `collection_loop`가 pending 시 page 노드·`save_elements` 스킵; `set_classifier` 안전망(저장 page B=∅이면 SUPERSET_MERGE 불가→DISJOINT라 빈 page가 sink 안 됨); `extract_interactable_indexes`를 root-inclusive(`tree.iter()`)로 수정해 단일 루트 interactable 누락 차단  (2026-06-30)
- Monkey-Collector: LLM element의 `description`/`parameters`가 디스크 저장 시 누락되던 문제 수정 — `ElementFamily`에 두 필드 추가(끝에, 하위호환), families 생성부가 ExtractedElement 5필드 전부 채움, `DataWriter.save_elements`가 `{step}_elements.json` 각 element에 직렬화(최종: name/description/parameters/element_index/key_element_index)  (2026-06-30)
- Monkey-Collector: world-modeling converter 프레임-이벤트 정렬 버그 수정 — action 라벨이 엉뚱한 프레임에 붙던 문제. action 이벤트에 `frame_index`(before 프레임 파일 인덱스 = `step_count - 1`) 기록, converter(`export/converter.py`)·offline page-graph 재빌드(`page_graph._load_events`)를 `step`(루프 카운터) 추측 대신 `frame_index` 직접 조인으로 교체(`_find_event_by_index` 폴백 제거). `state.step` 은 정상 action 에서만 `+1`(signal timeout·no_change·empty-UI·keyboard 등 비-저장 반복 13곳에서 제거 → step 미소비) + `idle_iterations` 절대 가드·stale `clear_signal_queue` 보강. `frame_index` 없는 구 세션은 변환 스킵·엣지 라벨 `unknown` degrade(재수집 필요)  (2026-06-30)

> 검증(2026-06-28): 위 OpenRouter LLM 통합을 실제 API Key + AVD(Pixel6-2)로 라이브 검증 — 정적 504 passed, 모델 슬러그(`qwen/qwen3.7-plus`) 실호출·화면 의미 그룹핑·문맥 입력 생성·비용 귀속·graceful fallback 동작 확인 (VERDICT PASS). 상세는 [DEVLOG.md](./DEVLOG.md).

<!-- project-sync: 릴리스/버전 변경 요약을 Added/Changed/Fixed로 한 줄씩 추가. -->
